# LSP Sequence Full 1K Report

Status: the requested 1000-step / 1K run has not been executed. This report is intentionally based on the PR13 `max_steps=100` gate run plus the PR14 minimal answer eval that loaded the PR13 step-100 checkpoint. No 1K metrics are inferred or fabricated.

## Inputs

- Train metrics: `/root/autodl-tmp/lsp_jepa/runs/pr13_mvp_full_seq_launch/max_steps_100/metrics.jsonl`
- Train summary: `/root/autodl-tmp/lsp_jepa/runs/pr13_mvp_full_seq_launch/max_steps_100/summary.json`
- Eval metrics: `/root/autodl-tmp/lsp_jepa/runs/pr14_mvp_min_eval/metrics.json`
- Config: `/root/SIM-CoT/lsp_jepa/configs/core/lsp_seq_full_train.yaml`
- Report generated by: `lsp_jepa/scripts/summarize_lsp_run.py`

## Training Configuration Summary

- experiment mode / host / backbone: `core` / `simcot` / `coconut`
- use_lsp_jepa: `True`
- data: `openai/gsm8k` `train`, expected samples `7473`, local path `/root/autodl-tmp/lsp_jepa/data/gsm8k_train.jsonl`
- observed gate run: `100` steps, batch size `16`, unique samples seen `1600`, max_steps field `100`
- optimizer config: lr `5e-05`, weight_decay `0.0`, gradient_clip_norm declared `1.0`, warmup_ratio declared `0.03`
- teacher: EMA decay `0.995`, update_trainable_only `True`, target_layer `last_2`, pooling `step_last_token`, target_space `projected_hidden`
- teacher leakage controls: exclude_answer_tokens `True`, exclude_answer_prefix `True`, answer_leakage_ok `True`
- student: latent_arch `latent_tokens`, latent steps `4`, predictor_head_layers `2`, detach_between_steps `False`
- objective / mapping: `lsp_state` with mapping `sequence`, target_position `final_valid_reasoning_step`
- losses: alignment `normalized_mse`, lsp_weight `1.0`, anti_collapse `variance` weight `0.01`, host_answer_ce_weight `0.1`
- disabled core contaminants: CODI distill weight `0.0`, SIM-CoT decoder weight `0.0`, intermediate CoT CE weight `0.0`

## Loss Curves

- total_loss: first 1.09287, last 0.949518, delta -0.143357, min 0.948414, max 1.09287, tail-10 mean 0.955503.
- lsp_loss: first 0.0303872, last 0.0191568, delta -0.0112304, min 0.0180593, max 0.0303872, tail-10 mean 0.0199978.
- host_answer_ce: first 10.5311, last 9.20959, delta -1.32148, min 9.2, max 10.5311, tail-10 mean 9.26039.
- anti_collapse_loss: first 0.00938066, last 0.00940196, delta 2.12956e-05, min 0.00926126, max 0.00954922, tail-10 mean 0.00946595.

Interpretation: total_loss, lsp_loss, and host_answer_ce decreased over the 100-step gate. anti_collapse_loss stayed finite and nearly flat, which is expected for the variance regularizer at this short horizon.

## Representation Diagnostics

- latent_variance_mean: first 0.0042059, last 0.0038353, delta -0.000370604, min 0.00209772, max 0.00623979, tail-10 mean 0.00302702.
- latent_variance_min: first 0.000731381, last 0.000904859, delta 0.000173479, min 0.000188478, max 0.00123486, tail-10 mean 0.000714127.
- raw_latent_variance_mean: first 0.100619, last 0.100969, delta 0.000349723, min 0.0675431, max 0.164709, tail-10 mean 0.0907533.
- pairwise_cosine_mean: first 0.899419, last 0.898706, delta -0.000713468, min 0.835005, max 0.952156, tail-10 mean 0.916168.
- pairwise_cosine_max: first 0.984219, last 0.999999, delta 0.01578, min 0.983303, max 1, tail-10 mean 0.997755.
- pairwise_cosine_min: first 0.450885, last 0.522337, delta 0.0714524, min 0.124956, max 0.760814, tail-10 mean 0.602932.
- pairwise_l2_mean: first 0.653182, last 0.639661, delta -0.0135207, min 0.466035, max 0.776336, tail-10 mean 0.569407.
- effective_rank: first 5.19119, last 5.42087, delta 0.229677, min 3.34689, max 7.17299, tail-10 mean 5.58393.
- pairwise_cosine_near_one batches: `0/100`; pairwise_cosine_all_one batches: `0/100`.

Interpretation: latent variance remained non-zero and the run did not report sustained pairwise-cosine near-one collapse. Pairwise cosine is high, and pairwise_cosine_max reaches almost 1 on some batches, so this should remain monitored, but the 100-step gate does not show a hard collapse signal by the recorded flags.

## Student-Teacher And EMA

- student_teacher_cosine: not recorded in PR13 metrics; no value is inferred.
- ema_drift_l1: first 0.817179, last 57.5764, delta 56.7592, min 0.817179, max 57.5764, tail-10 mean 55.6938.
- teacher_delta_l1: first 0.817179, last 57.5764, delta 56.7592, min 0.817179, max 57.5764, tail-10 mean 55.6938.
- teacher_grad_params last: `0`.

Interpretation: EMA drift is non-zero and increases through the gate, while teacher_grad_params remains zero in the final metrics, matching the no-grad EMA teacher contract.

## Gradient Diagnostics

- grad_norm: not recorded in PR13 metrics.
- student_grad_l1 substitute: first 36.2162, last 29.1825, delta -7.03371, min 29.1825, max 37.6808, tail-10 mean 29.9954.
- predictor_grad_l1 substitute: first 3.07059, last 2.42231, delta -0.648271, min 2.33381, max 3.13478, tail-10 mean 2.48874.
- student_base_grad_l1 substitute: first 33.1456, last 26.7601, delta -6.38543, min 26.7601, max 34.7009, tail-10 mean 27.5067.

Interpretation: because `grad_norm` is absent, this report uses the recorded L1 gradient diagnostics as substitutes. They are finite and non-zero; no spike or missing-gradient anomaly is visible in these substitute fields.

## Target And Failure Checks

- Non-finite numeric entries: `0`.
- Empty teacher target batches (`teacher_target_mask_nonempty=false`): `0/100`.
- Empty latent batches (`latent_mask_nonempty=false`): `0/100`.
- valid_final_targets: first `16`, last `16`.
- valid_teacher_targets: first `54`, last `57`.
- OOM: not indicated by the completed metrics/summary files; no OOM marker was recorded in the inspected JSON outputs.

## Answer Eval Results

- checkpoint: `/root/autodl-tmp/lsp_jepa/runs/pr13_mvp_full_seq_launch/max_steps_100/checkpoints/step_000100.pt`
- checkpoint step: `100`
- eval samples: `20` with limit `20`
- accuracy: `0`
- exact_match: `0`
- invalid_answer_rate: `1`
- generated_length_mean: `32`
- core eval contract: `{"adapter_expansion": false, "ema_updated": false, "no_teacher_branch": true, "step_level_eval": false}`

Interpretation: PR14 answer eval is clearly broken at this checkpoint: accuracy and exact match are 0, and every evaluated sample has an invalid extracted answer. Generated text contains mostly unknown-token outputs in the recorded examples. This blocks a positive step-level recommendation even though the representation diagnostics passed the short gate.

## Collapse Assessment

- Representation collapse: `not observed by PR13 100-step gate metrics` (latent variance non-zero, no near-one/all-one cosine flags, effective rank finite).
- Answer-generation collapse/failure: `observed` in PR14 minimal eval (invalid_answer_rate `1`, accuracy `0`).
- Teacher target collapse/emptiness: `not observed` in PR13 metrics; teacher target masks were non-empty for all recorded batches.

## Decision

- Sequence-level LSP-JEPA-Core training stability: `partial`. The 100-step gate is numerically stable and does not show recorded representation collapse, but the requested 1K/full run has not been executed and answer eval is broken.
- Recommend entering step-level now: `no`.
- Rationale: the project rule allows step-level only when loss is finite, latent variance is non-zero, pairwise cosine is not collapsed, and eval is not obviously broken. The first three conditions pass on the 100-step gate, but PR14 eval is obviously broken and no 1K run exists. First tune the sequence-level answer path, especially `host_answer_ce_weight` versus `lsp_weight`, then rerun a longer sequence-level gate before moving to step-level supervision.

## Logging Preference For Future Runs

Future training logs should emit a fixed-interval, tqdm-like progress line in the SIM-CoT training style. This is a logging/reporting preference only, not a new training algorithm. Each line should include at least step/total, total loss, lsp_loss, host_answer_ce, latent variance, EMA drift, elapsed time, and preferably throughput/ETA when available.

## Unresolved Items

- Execute the actual 1K/full sequence-level run before calling this a 1K result.
- Add or log `student_teacher_cosine` if it is required for future decision gates.
- Add `grad_norm` if exact norm-based anomaly checks are required; current report used L1 gradient substitutes.
- Investigate PR14 invalid-answer generation/tokenizer behavior and retune `host_answer_ce_weight` / `lsp_weight` before step-level.
